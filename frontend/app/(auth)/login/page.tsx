import Placeholder from "@/components/ui/Placeholder";

export default function LoginPage() {
  return <Placeholder title="Login" note="Phase 0 stub. Will POST to /api/auth/login via lib/auth.ts with credentials:include." />;
}
