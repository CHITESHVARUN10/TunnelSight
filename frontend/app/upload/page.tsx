import Placeholder from "@/components/ui/Placeholder";

export default function UploadPage() {
  return <Placeholder title="Upload PCAP" note="Will POST multipart to /api/analyze via apiForm(). Phase 0 backend stores a pending history row." />;
}
