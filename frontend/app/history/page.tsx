import Placeholder from "@/components/ui/Placeholder";

export default function HistoryPage() {
  return <Placeholder title="History" note="Will GET /api/history (per-user, session-scoped) and render past analyses." />;
}
