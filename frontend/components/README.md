# Components

Conversion from `stitch_screen/` is complete — pages carry their markup inline (verbatim port).

- `components/ui/` — shared bits added post-conversion (none yet; candidates: VerdictBadge, StatCard, ConfirmDialog if reused).
- `components/layout/` — reserved for a shared Sidebar/TopHeader if routes are later refactored (currently each page keeps its screen-accurate sidebar inline).
- `components/README_CONVERSION.md` (deleted) — superseded by `frontend/README.md`.
