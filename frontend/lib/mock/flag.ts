// Prototype switch. true = frontend-only demo (mock data, localStorage session).
// When the FastAPI backend is ready, flip to false and pages call lib/api.ts.
export const USE_MOCK = true;

export const DEMO_EMAIL = "analyst@enterprise.internal";
export const DEMO_PASSWORD = "tunnelsight-demo";
