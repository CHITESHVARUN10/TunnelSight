// Prototype switch. false = real FastAPI backend via lib/api.ts (session cookie).
// Offline demo fallback: set NEXT_PUBLIC_USE_MOCK=true to allow mock data when the network fails.
export const USE_MOCK = false;

export const DEMO_EMAIL = "analyst@enterprise.internal";
export const DEMO_PASSWORD = "tunnelsight-demo";
